#include "main.h"

/**
 * _islower - must check for lowercase character
 * @c: be the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
